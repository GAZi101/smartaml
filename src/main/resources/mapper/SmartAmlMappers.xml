<?xml version='1.0' encoding='UTF-8' ?>
<!DOCTYPE mapper PUBLIC '-//mybatis.org//DTD Mapper 3.0//EN'
        'http://mybatis.org/dtd/mybatis-3-mapper.dtd'>

<mapper namespace='com.tridaya.smartleads.mapper.SmartLeadsMappers'>

    <select id="getAjukanKredit" parameterType="int" resultType="Map">
  	SELECT
     * FROM tbl_ajukankredit WHERE tak_id=#{param,mode=IN,jdbcType=INTEGER}
  </select>
</mapper>